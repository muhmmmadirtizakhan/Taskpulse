{
  "name": "taskpulse",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "PORT=${PORT:-3000} node server.js",
    "compile": "bash compile.sh",
    "build": "npm install && bash compile.sh"
  },
  "dependencies": {
    "express": "^4.22.1",
    "cors": "^2.8.6",
    "@supabase/supabase-js": "^2.95.3"
  },
  "engines": {
    "node": "18.x"
  }
}